name: Test action
on:
  push:
    branches:
      - 'main'
  pull_request:
  workflow_dispatch:

jobs:
  test_simple:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: ./
      with:
        vm_image: ${{ secrets.VM_IMAGE }}
        ssh_key: ${{ secrets.VM_KEY }}
    - shell: macppc {0}
      run: |
        whoami
        uname -a
        date
